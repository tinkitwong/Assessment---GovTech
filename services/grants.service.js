const db = require('../models')
const grants = db.grants
const Person = db.person
const Op = db.Sequelize.Op

module.exports = {
    // findAll households based on provided options / attributes 
    BSG : (household) => { 
        try {
            // Household with young children younger than 5.
            
        } catch (error) {
            return error
        }
    }, 
}